# Products

No product implementations are present yet.

Bootstrap reservations:

- `products/cpk_server`: the control-plane process wrapper, implemented only
  when #813 begins.
- `products/hello`: the first ordinary reusable server product, implemented only
  after the Hello law-context and translation issues begin.

Do not add empty product implementation directories before their issue opens.
The machine-readable inventory lives in `coordination/product-inventory.json`.
