{
  "schema": "cpk-server.bootstrap-contract",
  "environment": [
    {
      "name": "CPK_SERVER_MODE",
      "required": true,
      "allowed_values": [
        "execution-capable"
      ]
    },
    {
      "name": "CPK_CONTROL_AUTH_VERIFIER",
      "required": false,
      "allowed_values": [
        "none",
        "static-development"
      ]
    },
    {
      "name": "CPK_CONTROL_AUTH_STATIC_CREDENTIAL",
      "required": false,
      "allowed_values": [
        "runtime-secret-delivered-development-credential"
      ]
    },
    {
      "name": "CPK_CONTROL_AUTH_STATIC_WORKSPACE_GRANTS_JSON",
      "required": false,
      "allowed_values": [
        "trusted-exact-workspace-to-closed-scope-map"
      ]
    },
    {
      "name": "CPK_PORT",
      "required": true,
      "allowed_values": [
        "1..65535"
      ]
    },
    {
      "name": "CPK_RUNTIME_INTERPRETERS",
      "required": true,
      "allowed_values": [
        "none",
        "docker"
      ]
    },
    {
      "name": "CPK_INGRESS_INTERPRETERS",
      "required": false,
      "allowed_values": [
        "none",
        "cloudflare"
      ]
    },
    {
      "name": "CPK_GATEWAY_PROBE_SIGNER",
      "required": false,
      "allowed_values": [
        "none",
        "ed25519"
      ]
    },
    {
      "name": "CPK_PUBLIC_DNS_RESOLVER_ENDPOINT",
      "required": false,
      "allowed_values": [
        "trusted-https-dns-over-https-endpoint"
      ]
    },
    {
      "name": "CPK_PRODUCT_MATERIAL_RESOLVER",
      "required": false,
      "allowed_values": [
        "none",
        "provider",
        "local-development"
      ]
    },
    {
      "name": "CPK_PRODUCT_SECRET_VALUES_JSON",
      "required": false,
      "allowed_values": [
        "runtime-secret-reference-to-value-map"
      ]
    },
    {
      "name": "CPK_MATERIAL_PROVIDER_ROUTES_JSON",
      "required": false,
      "allowed_values": [
        "opaque-endpoint-reference-to-base-url-map"
      ]
    },
    {
      "name": "CPK_MATERIAL_PROVIDER_BOOTSTRAP_FILES_JSON",
      "required": false,
      "allowed_values": [
        "bootstrap-credential-reference-to-mounted-file-map"
      ]
    },
    {
      "name": "CPK_WORKPLACE_DATABASE_URL",
      "required": true,
      "allowed_values": [
        "runtime-provided-postgres-url"
      ]
    },
    {
      "name": "CPK_ACTIVITY_HISTORY_DATABASE_URL",
      "required": true,
      "allowed_values": [
        "runtime-provided-postgres-url"
      ]
    },
    {
      "name": "CPK_OBSERVER_STATE_DATABASE_URL",
      "required": true,
      "allowed_values": [
        "runtime-provided-postgres-url"
      ]
    },
    {
      "name": "CPK_GRAPH_TOPOLOGY_DATABASE_URL",
      "required": true,
      "allowed_values": [
        "runtime-provided-postgres-url"
      ]
    }
  ],
  "runtime_inputs": [
    "authorization credential and static-development verifier credential are runtime-supplied and never echoed",
    "static-development workspace grants are explicit exact-workspace authority and never inferred from request payloads",
    "runtime interpreter authority is selected explicitly and never inferred",
    "ingress interpreter authority is selected explicitly and never inferred",
    "gateway probe signing authority is selected explicitly and resolves only its secret reference at dispatch",
    "public DNS resolution uses bounded fresh queries against a trusted process-bootstrap endpoint that is never echoed",
    "store endpoints are runtime-supplied and never echoed by readiness",
    "provider endpoint references map to bounded process-local routes and are never echoed",
    "provider bootstrap credentials are mounted files read only by the provider client and are never recursively resolved through operations",
    "product secret authority is admitted by operations and resolved only at interpreter dispatch",
    "local-development secret maps are explicit fixtures and are not selected by production descriptors"
  ]
}
