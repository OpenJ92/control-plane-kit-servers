# Server application-readiness integration

This isolated roadmap begins at accepted server source
`8923679b2b0724c976215c227850ce297c444d7c`, the retained
`roadmap/1752-product-loop` tip after PR147. Its 29 commits beyond develop
`6fbfa04b5fe9a087dceba6709c4d9843e483dbf4` belong to the already accepted 1752
client work, not new #148 delivery. Server PR140 remains its open integration
dependency; reconcile that promotion before this roadmap reaches develop.

New branch: `roadmap/1754-application-readiness`, draft PR into develop.
Child: `codex/148-revision-history-composition`, one implementation PR into this
new roadmap. The retained1752 branch, PR140, checkout and installation do not
advance as a side effect.

Server #148 consumes CPK #1773 owner merge
`73f645d506ad6c2f79cfeff4bc6869a7f4cf6980`. Core700 and Operations1522 owning
tests, compilation/import, hosted checks, Meridian and Kepler reviews passed
upstream. Server-owned composition validation is still pending.

The expected change is the canonical CPK dependency coordinate and its generated
mirrors, existing protocol/composition witnesses, and proportional installed
HTTP/MCP evidence. No new server history queries or semantics are justified by
source fit. Empty saved-revision pages prove packaged composition; the accepted
owner tests remain the evidence for nonempty membership, pagination and receipts.

Security/data: no new authority, runtime adapter, credential, image publication,
provider action, migration/reset/backfill, retained schema mutation, live
deployment, ingress change or #1774 work. Disposable package smokes are the only
planned runtime validation. Existing images and all unrelated coordinates stay
fixed. Package availability does not imply live rollout.

Vale implements; Meridian reviews exact tests/red/source/green; Kepler reviews
the integrated result; North controls topology and merge. Before execution,
review tests-only bytes and use only the established Docker-backed `./test.sh`.
Do not repair unrelated residue or unavailable prerequisites. This initial
documentation checkpoint is validated only with `git diff --check`.
