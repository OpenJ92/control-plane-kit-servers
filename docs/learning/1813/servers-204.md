# Servers #204: maintained Secrets source smoke

Parent #189, accepted predecessor #203/PR205. Source starts from accepted roadmap
merge f8411aa and targets roadmap/1813-runtime-control. This slice restores the
maintained standalone source path; immutable publication and public grandparent
acceptance remain separate. North's issue decision records the calibrated
implementation/proportional-test/full-owner-gate loop without another mandatory
target-red run. This is adapter restoration, not a parity migration.

## Laws and source decisions

Preserve all six accepted product-contract laws and original provider private
bootstrap, custody generate/replay/resolve/revoke and retained-value restart
assertions. Replace the obsolete temporary source hold with pre-effect profile
admission and real source acceptance. Unknown/contradictory flags and malformed
historical images fail before Docker or traps. Default source and legacy explicit
BUILD_IMAGE=0 historical behavior remain distinct; the normal gate forces both.

The actual product factory and Secrets codec own public configuration. A small
product fixture reuses accepted authority values, writes one public artifact and
two private headers per startup, and keeps signing keys in memory. Stable target,
runtime and declaration get fresh synthetic keys/grants at restart. This is test
input, not production rotation or automatic recovery. Checks reconstruct requests
from the phase's decoded value; no replacement authority or denied-token refresh.
Provider mounts only the public file and inherits recipe ENV. Private/data mounts
and original custody programs stay intact. Source startup is now create then start
so the labelled resource exists before execution.

## Security and operational review

Independent design review found an existing failure-path disclosure: non-quiet
grep could print a matching secret log line. Both profiles now retain the same
marker/exact-private detection through bounded non-echoing checks. Fixed0600 log
capture is limited to one MiB plus the overflow byte; independent producer status
prevents a failed Docker log read from becoming clean input. Overflow is failure,
not accepted truncation. Source tokens are read from private files, never argv.

The host owns0700 temporary phase directories and generated444/600 files through
an explicit generator UID/GID. Check/log controllers have read-only mounts and no
Docker socket. Cleanup enumerates both log files and all three files per known
phase, including partial generation. It preserves the recorded path on failure
and retains existing labelled Docker cleanup/absence checks. No broad prune.
HTTP timeouts are per operation; no hard Docker or total-request deadline is
claimed. Existing readiness bounds precede immediate120-second signed checks.

## Test context and current evidence

Meridian and Kepler independently passed the refined design/law/scope plan;
North released source implementation only. Existing #203 product laws remain
unchanged. Proportional admission tests replace the hold test; new helper tests
protect public/private file shape and per-phase identity, fixed non-echoing log
failures including overflow/status errors, and partial exclusive creation.
Actual HTTP/custody/restart evidence belongs to both maintained smoke profiles
inside the normal owner suite, not a mocked receiver matrix.

This source checkpoint is unvalidated: no executable tests, Docker, push, PR,
hosted workflow or registry action has run for #204. Full source/fixture/effect
review and exact full-command release must precede runtime validation. Parent
#189 remains open. Final review must verify actual shell status propagation,
cleanup and forced two-profile execution; design approval is not runtime proof.
