# Runtime control integration: source and acceptance handoff

This roadmap starts from accepted Servers
`ba7ebc82edb37519043db544eca4a731d0a3560b` on
`roadmap/1754-application-readiness`. Its branch is
`roadmap/1813-runtime-control`; its draft PR targets roadmap1754, whose PR149
retains the develop integration boundary. Held Servers #163 source and runtime
resources are not this branch's base.

Root [Core #1813](https://github.com/OpenJ92/control-plane-kit/issues/1813)
and phase [#1816](https://github.com/OpenJ92/control-plane-kit/issues/1816)
govern the ordered owner work. The first child is
[Servers #193](https://github.com/OpenJ92/control-plane-kit-servers/issues/193):
canonical dependency alignment, followed by independently released Python
product adoption. Native wrappers retain their Python-first prerequisites.

## First dependency handoff

Interpreters #150 / PR151 is accepted at merge
`77c9a7f54e6d8ef886733c8ebd3476e796fad8ad`: 26 policy and 337 package tests,
exact Core installation, local synthetic-secret access/cleanup witness, hosted
checks and independent review passed upstream. SDK #21 is complete at
`2b10d5a354ba4da9407d336703aacb96910100d4`. Both select Core
`95452249d0340707a5cdffe737e34669e9d53165`; Operations source is unchanged
between this Core revision and the previous Servers selection e3e29995.

Child #193 must make these dependencies coherent through
coordinates/server-products.json and its generator, adding explicit root
SDK[verification] ownership. Product-specific SDK installation and composition
wait for their wrapper issues. Upstream package tests are prerequisites, not
proof of the Servers Python3.12 installed combination.

## Source and acceptance gates

Each child has its own ordinary Docker-backed ./test.sh evidence, independent
review and North's merge decision. The root integration stays draft through
remaining #1813 owner/source gates and actual adopted candidate composition.
Final root closure also requires public acceptance; source readiness must not
depend cyclically on held #163 acceptance before prerequisite code can merge.

Servers #193's release includes the existing package/local Docker gate with
synthetic material, designated daemon controllers, port18089 and exact owned
cleanup. Missing/occupied prerequisites or uncertain effects stop execution for
coordination; do not alter the gate or repair shared state. Existing published
image stages prove baseline coexistence. Candidate wrapper image qualification
belongs to #191; published coordinates do not acquire new capabilities merely
because source dependencies advance.

Servers #193 is accepted at merge74999efd after complete owning/hosted gates and
independent review; its initial two stale expected-pin failures remain recorded.
Hello #184 is released from that merge with product-local trusted configuration,
fixed internal8000 declaration, immutable instance settings, finite dependency
inputs and a five-second cooperative observation budget. It preserves TCP-only
Postgres meaning and cannot claim a hard deadline. Receiver/source tests and a
generated SDK recipe remain distinct from #191 image qualification and producer
artifact delivery. Its source/acceptance decision log is the child PR; no new
public product descriptor is published by this slice.

Security/data: this integration document creates no runtime authority,
credential custody, public exposure, durable mutation, migration or provider
action. Product source/image coordinates, original tunnel/DNS/token, held
resources and prior history are preserved. No image publication is released.

Vale implements; Meridian independently reviews source/test/evidence; Kepler
reviews dependency/architecture coherence; North owns topology and merge.
This initial documentation checkpoint has only git diff --check validation.

Hello184 is accepted at merge6ad7d8ba. Router185 follows from that exact base on
codex/185-active-router-control-wrapper with Meridian/Kepler DESIGN PASS and
North's bounded source/GitHub release. A review correction distinguishes canonical
/__control/health/readiness from unknown /__control/health/ready; ordinary
/health/ready remains forwarded. Product-local V2 control advertises liveness only,
with no upstream assertion or switching. The actual stdlib host is passive until
SDK installation, with separate instance authority and settings; production
port8000 matches the source contract. Historical descriptor/policy/image remain.

The focused law commit a3b1412 precedes application implementation; no compulsory
red harness was run. Normal owning validation and independent source/evidence
review are pending at this source checkpoint. Recipe generation adds the router's
SDK[verification] install; this does not qualify a router OCI. Common receiving
mechanics are now visible in a second product, but no cross-product import or
shared configuration framework was introduced. Producer artifact delivery and
public gateway acceptance remain their own later boundaries.

Router185 is accepted at merge3e3ee672 after both final reviews,26 policy and364
package tests plus complete local/hosted gates and cleanup. Multiplexer186 follows
from that exact base under the reviewed product-local liveness-only design.
Focused law commit f5945e9 precedes source implementation. Source checkpoint:
normal owning validation and independent implementation/evidence review pending.

The multiplexer preserves a distinct transformation: primary failure returns
fixed502 without observers; primary success precedes sequential best-effort
observer delivery and remains the authoritative response. Fail-open does not
remove latency. Signed control routes reach neither lane; no readiness probe,
queue, switching, shared configuration framework or producer delivery is added.
The canonical recipe installs SDK[verification] while historical image coordinates
remain. The separate legacy image smoke lacks receiving config/owned dynamic
cleanup and is neither run nor repaired here;191 owns its candidate qualification
plan. Root/gateway/public acceptance and1821/149 delivery remain distinct.

Multiplexer186 is accepted at64e531549f3a623a6a6b4d883f988c09d38a4c53.
The reviewed CPK183 design splits into199 hosting/dependencies then held200
receiving configuration/production health/source-smoke integration. North releases
199 only. Its law checkpoint d87c670 precedes source implementation; no separate
red harness runs. All74 routes retain the neutral boundary through contract-derived
literal prefixes. Public health/MCP fallthrough and a scoped framework404/405
projection preserve legacy routing while real SDK composition owns __control.

Root and CPK recipe now select SDK[fastapi]2b10d5a, including exact
FastAPI0.141.1/Starlette1.6.0. The existing generator already rewrites SDK archives
in the CPK destination; tests now establish that actual dependency/mirror.
No schema/service, receiving-file requirement, descriptor/image coordinate or
normal smoke changes occur. Source implementation review and the normal owning
gate are pending at this checkpoint. Test-only installed health proves host
composability, not a deployed wrapper. Child200 will install admitted control
before schema effects and supply explicit owned source-image fixture authority.

The first hosted run34735431619 atc65262e passed policy/root checks and five
new host tests, then failed two parity assertions because wire JSON encodes
principal workspace_grants tuples as lists. Expected complete responses now
undergo standard JSON serialization before comparison; no field/status/order
assertion or production code is weakened. No local gate has run.

Servers199 merged atdcae8af6ac1564b29eec8361c7a28426debd1263 after both final
PASS reviews, one local exit0 gate (26policy/380package) and matching hosted green.
The earlier tuple/list assertion failure remains historical. North releases200
from that merge. Focused receiver laws are committed at1a6e700 before application
implementation. No executable validation has run at this source checkpoint.

Current CPK source now requires its own typed public receiving artifact. All three
full source contracts retain their prior operational topology and add only actual
configuration plus liveness V2 control. The SDK host and instance authority are
admitted before Operations schema effects; the initialized app returns afterward.
No readiness probe, variable workflow, producer delivery or new public listener.

Source smoke is explicitly wrapped-source with synthetic authority generated
inside the existing no-network test/controller process after build/PG preparation.
Private keys stay in memory, grants live240 seconds, public JSON is0444 mounted
alone, and0600 header/response files stay in a0700 directory with exact cleanup.
Published-baseline retains the historical image path. North requires sealed local
source/fixture review before push because hosted CI starts the modified gate;
no automatic push or validation is dispatched by this checkpoint. Source and gate
reviews, complete retained/hosted evidence and parent183 acceptance remain pending.

North's pre-push review found that a foreground missing-control rejection could
hang if enforcement regressed. The new probe now tracks its labelled container
ID before start, polls at most15 times, captures at most4096 bytes/10 log lines,
and includes the ID in existing cleanup. Production behavior is unchanged; no
validation was attempted to discover this gap. Corrected source review follows.

The negative probe bounds server-state polling under a responsive Docker daemon;
it does not impose a hard wall-clock deadline on a hung Docker CLI call.

First released hosted34736979622 at53eed4d failed a frozen gate-policy assertion
before package/image/fixture stages: added environment assignments split the
required real CPK_SERVER_BUILD_IMAGE=1 sh invocation. The corrected ordering puts
source profile/test-image assignments before that unchanged forced-build command.
Environment, effects and shared policy assertions remain unchanged; no support
repair or local gate was attempted. Corrected hosted release/review is pending.
