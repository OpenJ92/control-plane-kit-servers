# Servers207: gateway health trust and pure consuming verifier

Accepted base2e4f391d30aaa2db095efd466ac5915aa34ed8f9; branch targets
roadmap/1813-runtime-control. Main/develop were older than this vertical and
were rejected before edits. Existing Core95452249 transit contracts suffice;
no dependency or gate adjustment is needed.

Design/law cards: issue207 comment5689740764; both designPASS5689760259.
Target-onlyc360af8 was held by both reviewers because invalid oversized values
could fail for reasons other than their claimed limits. Amended8b05ff4 retained
all malformed cases and added valid whitespace-padded config/JWS boundaries,
16/17distinctkeys and at-cap positives. Both amended staticPASS; no skipped or
weakened assertions.

Native target-red run35038244164/job104612088809 tested82a91b9e453c9bcc6dd4ca5289d14bedaf2456b0
merging exact8b05ff4 into2e4f391d.26support+74prior package methods passed;
all12new methods reached missing-configuration-module assertions,0errors.
Core/crypto setup earned credit, deeper assertions and later suite stages did
not. Evidence PR209comment5689866072; both causalPASS. North then released the
two source modules and documentation, preserving targets/pins/probe/routes.

The useful boundary is exact selected artifact -> sole strict product decoder
-> actual pure verifier, with independently supplied per-call execution
context. A fresh content digest is not semantic proof: changing configA toB
must change signature acceptance. OverlapA+B preserves key coverage. Expected
gateway/workspace/runtime/issuer come from configuration; target/declaration/
kind/attempt/time stay per-call. No Core graph field, Operations import, probe
skew/cache, HTTP handler, new schema or provider effect is introduced.

The new compact envelope is a public receiver contract for149/180. Its typ,
claims, signature input and bounds are documented in the verifier companion.
Two modules remain one product-owned vertical because the verifier is the
actual consumption witness for the configuration; an unsigned fixture could
not prove that relationship.

Source checkpoint: implementation committed for independent review; native
green pending. No live/source-process wiring/image adoption is inferred.
Next owners1857/208 must bind exact pinned product/profile/slot/selected bytes;
180 retains process wiring and HTTP relay. Runtime delivery remains separate.
