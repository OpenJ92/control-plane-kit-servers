Source: [tests/test_package_metadata.py](../../../tests/test_package_metadata.py).

Early #181 compatibility for #208 selects Coreb79a02d1, Operationsbc559a0, Interpreters6b1c75a9, SDK2c5b5882 and Secrets8273b7de through independent canonical Core/Operations fields. The ordinary gate checks generated bytes and installed provenance; this is source dependency adoption, not image publication or production health execution. Historical product descriptors/image coordinates are unchanged.
Maintain this companion with root packaging and dependency adoption.

Metadata tests require exact Core, Operations, Interpreters, Secrets and SDK[fastapi]
requirements from the canonical manifest. The isolated installed process reads
each distribution's direct_url.json and compares archive URL/subdirectory to
those coordinates, then imports SDK stdlib, FastAPI, health and verification interfaces. The actual installed
FastAPI0.141.1/Starlette1.6.0 versions are checked.
This runs inside the owning Python3.12 Dockerfile.test environment after normal
pip resolution. Missing distributions, wrong pins or unavailable optional crypto
imports fail the test; no fallback installation or local override is attempted.

Existing lightweight root/catalogue and process-import separation assertions
remain. The import witness starts no SDK listener and does not duplicate SDK
authorization matrices. Installed source compatibility is distinct from a
product's Dockerfile composition, published digest or health capability.

Servers203 adds actual Secrets source provenance and verifies cold root/catalogue import does not load Secrets or its product implementation. Installing the dependency makes the public codec available; it does not authorize a provider process or import custody at the catalogue entrance.
