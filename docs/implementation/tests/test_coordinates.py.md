Source: [tests/test_coordinates.py](../../../tests/test_coordinates.py).
Maintain this document alongside its source file. When the source or relevant imported contracts change, verify and update this companion in the same change.

These tests load the coordinate scripts as modules and compare generated bytes with the maintained repository files. They check synchronization of selected dependency pins, digest-based execution references, canonical source commits and distinct published/build coordinates. Selected smoke-script text must consult the helper rather than hardcode another digest.

Generation here returns an update map; the test does not apply changes or push/pull images. Repository equality and hardcoded expected coordinates establish declared consistency, not registry existence, publication provenance or current provider truth. Coordinate adoption must intentionally update affected expectations while retaining that evidence distinction.

Related source and evidence: [scripts/apply_coordinates.py](../../../scripts/apply_coordinates.py), [scripts/product_image_coordinate.py](../../../scripts/product_image_coordinate.py), [coordinates/server-products.json](../../../coordinates/server-products.json).
