Source: [pyproject.toml](../../pyproject.toml).

Servers #228 adds the cloudflared connector implementation namespace to package
discovery for its bounded native reader. This is product-owned standard-library
code, with no SDK/process startup on import and no dependency-coordinate change.

The September 26 #181 dependency adoption selects Core and Operations f1e6cf2 through separately owned archive subdirectories, reviewed Interpreters4bb9d857, SDK22f1267b and Secrets43b742d1. The canonical upstream fields and generated installation mirrors agree; the unchanged owner gate checks generated bytes and installed provenance. Product descriptors, source coordinates and image digests remain unchanged. This is source dependency adoption, not completed process composition or live qualification.
Maintain this document alongside its source file. When the source or relevant imported contracts change, verify and update this companion in the same change.

This distribution includes the shared catalogue package and selected product implementation namespaces, and exposes the cpk CLI through the cpk-server client entrypoint. Package-data includes the generated catalogue and checksum. It does not start those product processes when the lightweight root is imported.

Core and Operations retain independent coordinate fields, currently both f1e6cf2
with their own package subdirectories. Accepted Interpreters4bb9d857,
SDK22f1267b and Secrets43b742d1 share the exact Core archive. SDK[fastapi]
selects FastAPI0.141.1 and Starlette1.6.0; Uvicorn and build dependencies still
use ranges, so exact CPK pins do not make the whole environment a lockfile.
Selected software availability does not wire the new awaited Operations path.

Dockerfile.test installs this combination on Python3.12. The owning package metadata test verifies installed direct-URL provenance for all five distributions in an isolated Python process and imports SDK verification/health/stdlib/FastAPI surfaces and checks the installed FastAPI/Starlette versions. This establishes installed source compatibility without starting a listener or granting authority. Product-specific Dockerfile SDK installation remains with each wrapper issue; this root package dependency does not qualify an existing published image.

Related source and evidence: [coordinates/server-products.json](../../coordinates/server-products.json), [scripts/apply_coordinates.py](../../scripts/apply_coordinates.py), [src/control_plane_kit_servers/__init__.py](../../src/control_plane_kit_servers/__init__.py), [tests/test_package_metadata.py](../../tests/test_package_metadata.py).

Servers #177 consumes the reviewed CPK secret-delivery selection/admission correction through this generated pin. The installation composition test exercises the real composer and public Operations translator with default and substituted references. This packages future material behavior; it neither authorizes runtime effects nor changes persisted intents.

Servers203 introduced the explicit Secrets dependency and packaging of products/secrets_server/src. This product owner imports only the actual public receiving codec and frames its value for topology. The lightweight catalogue/root remains independent of the service/process. Secrets selects the same Core/SDK pins already adopted here; its custody implementation is not copied into Servers.

#180 declares HTTPX directly (`>=0.27,<1`) for the gateway's bounded async health
relay, rather than relying on a transitive client dependency. Immutable CPK
package coordinates remain unchanged. This does not lock the entire transitive
resolution or publish an image.
