Source: [coordinates/server-products.json](../../../coordinates/server-products.json).

The September 26 #181 dependency adoption selects Core and Operations f1e6cf2 through separately owned archive subdirectories, reviewed Interpreters4bb9d857, SDK22f1267b and Secrets43b742d1. The canonical upstream fields and generated installation mirrors agree; the unchanged owner gate checks generated bytes and installed provenance. Product descriptors, source coordinates and image digests remain unchanged. This is source dependency adoption, not completed process composition or live qualification.
Maintain this document alongside its source file. When the source or relevant imported contracts change, verify and update this companion in the same change.

This is the maintained input for upstream source pins and published product image/source coordinates. The three cpk-server descriptor variants share one image coordinate and implementation directory while retaining distinct product IDs and descriptor paths. A variant is not a separate build merely because it has its own descriptor.

The declared source commit, selected dependency commits and image digest are separate evidence coordinates. They must be updated through actual reviewed adoption/publication, not inferred from the current checkout or a moved tag. The coordinate helper renders registry/repository@digest for execution and discards the mutable tag for that purpose. This file contains declarations, not a registry query, image-account seal or proof that any deployment ran.

Related source and evidence: [scripts/apply_coordinates.py](../../../scripts/apply_coordinates.py), [scripts/product_image_coordinate.py](../../../scripts/product_image_coordinate.py), [tests/test_coordinates.py](../../../tests/test_coordinates.py).

The former shared Core/Operations field was split by the early181 prerequisite.
Both fields remain required canonical lowercase 40-character commits, and may
select the same commit with different package subdirectories. Current
Interpreters, SDK and Secrets select the identical Core f1e archive. Root SDK
uses its FastAPI extra while product recipes retain their own extras. Generated
destination equality and installed direct-URL provenance remain the evidence;
the separate historical product source/image coordinates do not advance.

#221 historically selected Interpreters388282ae42d3a5629707d24630cc079664112636
through this canonical input and the existing generator's root/CPK recipe
outputs. Coreb79, Operationsbc559, SDK2c5 and Secrets827 remain fixed. This
isolated diagnostic composition supplies the existing149 signer/147 client;
it does not select newer164/e074 work or change published image/catalogue
coordinates. The unchanged gate must validate generated bytes and installation.
