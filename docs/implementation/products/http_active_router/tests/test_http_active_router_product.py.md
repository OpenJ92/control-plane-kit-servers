Source: [test_http_active_router_product.py](../../../../../products/http_active_router/tests/test_http_active_router_product.py).
Maintain with the nine inherited router laws.

All nine tests and their assertions survive wrapper adoption. Only source checks
for ACTIVE_TARGET_URL and scheme validation follow settings into configuration.py;
forwarding checks remain on server.py. The explicit PORT18080 settings test is
preserved, while the separate wrapper suite verifies fixed production8000.
Historical descriptor identity, digest, active requirement, complete verification
policy and process-independent loading remain governing baseline behavior.
