# Health relay test fixtures

Synthetic distinct Ed25519 keys issue original-window Core transit/workload grants.
The receiver uses the installed SDK's public route installer, real verifiers and
protected callbacks. No provider material, network deployment or production
catalogue is used. ReceiverTransport connects only to that ASGI receiver and
records outbound requests/cleanup.

The edge-free graph describes a persistent wrapper's distinct application HTTP,
SQL5432 and management HTTP8087 providers. The actual Operations projection selects
the management surface; the fixture does not grant dispatch authority. Two
prerequisite tests validate real receiver acceptance/denial and the projection
before missing #180 interfaces can supply red evidence.
