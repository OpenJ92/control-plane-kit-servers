Source: [__init__.py](../../../../../../products/secrets_server/src/control_plane_kit_servers_secrets_server/__init__.py).
Maintain with the source file.

The installed product namespace has no eager exports or imports. Product values are selected explicitly from configuration; the Secrets service owns its process, private files and custody. The shared catalogue does not import this product implementation.
