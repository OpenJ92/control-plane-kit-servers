Source: [test_secrets_server_product.py](../../../../../products/secrets_server/tests/test_secrets_server_product.py).
Maintain with the published product contract and maintained smoke boundary.

All six historical product laws remain: canonical descriptor identity/roundtrip/image provenance, HTTP8081 and retained data, nonrecursive private0400 inputs with empty historical artifacts/deliveries, numeric recipe user/source pin, full legacy smoke behavior and Core instantiation. Source contracts have their own focused tests; the old published artifact-empty assertion is not weakened.

#204 replaces the temporary source hold law with pre-effect rejection of unknown
or contradictory profile/build flags and malformed historical immutable images.
The actual shell runs with the existing temporary Docker-command observer and
ten-second subprocess bound inside the normal socket-free package runner.
Assertions require exit2, fixed error and no Docker invocation, including cleanup.
The existing smoke contract method also checks forced source and historical gate
wiring. Successful profile interpretation belongs to the two full-gate smoke
invocations, not an emulated provider or replacement harness.
