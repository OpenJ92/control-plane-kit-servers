Source: [test_secrets_server_product.py](../../../../../products/secrets_server/tests/test_secrets_server_product.py).
Maintain with the published product contract and maintained smoke boundary.

All six historical product laws remain: canonical descriptor identity/roundtrip/image provenance, HTTP8081 and retained data, nonrecursive private0400 inputs with empty historical artifacts/deliveries, numeric recipe user/source pin, full legacy smoke behavior and Core instantiation. Source contracts have their own focused tests; the old published artifact-empty assertion is not weakened.

One temporary203 law runs the actual shell script in default and explicit source-build modes with a failing Docker command observer on PATH. It requires bounded exit2 and the explicit204 handoff before any observed Docker call, including trap cleanup. This is a shell admission test inside the normal socket-free package runner; it creates only temporary observer/marker files and a10-second subprocess, no actual Docker resource.204 replaces this temporary hold law when it restores supported source behavior. The historical immutable smoke still runs in the full owning gate.
