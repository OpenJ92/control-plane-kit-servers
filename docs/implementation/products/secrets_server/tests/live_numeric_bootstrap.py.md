Source: [live_numeric_bootstrap.py](../../../../../products/secrets_server/tests/live_numeric_bootstrap.py).
Maintain with the actual source recipe, interpreter materialization and owning gate.

This existing designated Docker controller runs inside the owning suite, checks its expected engine/network/image labels, and tracks exact resources before use. The source Secrets image is built by the normal gate. Existing numericUID10006/account/HOME/PID, private0400 actual delivery/read-only/otherUID denial, retained writable data, provider auth and redaction laws remain. CPK's UID10001 private recipient probe is unchanged.

The product helper now produces an actual public artifact from synthetic authority. One additional labelled volume is appended to the existing resource list before materialization. Existing DockerSdkClient materialize_configuration_artifact/digest and DockerSdkConfigurationMount deliver only public content, with their helper IDs included in existing absence checks. The provider receives that read-only mount alongside unchanged private/data mounts; no environment or command override supplies the path. Actual Config.Env must contain the recipe's exact variable/path, and actual file checks require regular0444/read-only bytes matching the public artifact digest.

After normal startup and provider checks, the same controller mints fresh120-second typed static/health grants from separate in-memory private keys. Same-service HTTP requests use5-second HTTPX operation timeouts and accumulate at most65536 response bytes in4096-byte chunks; this is not a hard total request/daemon deadline. Actual Core canonical/typed results and missing/wrong-purpose401 outcomes are checked. Tokens join the existing sensitive list before requests so success-path bounded log checks include them; no new credential files, issuer/probe container or authority refresh occurs.

All added resources/helpers join the existing reverse cleanup and exact absence proof. The unchanged controller/engine/cleanup mechanisms retain their limits under ambiguous provider/daemon failures; no automatic retry or shared cleanup is added. The bounded result includes public_control and signed_control_reads only after those checks pass. This is product source composition, not production delivery or image qualification, and no new execution is authorized by this note.
