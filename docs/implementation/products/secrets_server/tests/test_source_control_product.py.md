Source: [test_source_control_product.py](../../../../../products/secrets_server/tests/test_source_control_product.py).

The September 26 #181 adoption updates exact selected dependencies to Core/Operations f1e6cf2, Interpreters4bb9d857, SDK22f1267b and Secrets43b742d1. Installed provenance and published-image boundaries remain unchanged.
Maintain with the source file and actual imported contracts.

Six product-owned laws require the installed artifact/source-contract entrance before importing it. This makes absent product behavior an explicit assertion with clean collection; no stub or collection/import failure supplies red evidence. Downstream assertions prove exact service-encoded bytes and artifact framing, full original runtime-field preservation with only the declared public/control additions, actual Core codec/instantiation, invalid framing/service input and fixed context-free errors, recipe/public-bootstrap agreement with unchanged private/published coordinates, and isolated installed dependency/public-import boundaries.

The full-contract test compares all fields after removing only the intended additions, and explicitly preserves both health paths/statuses and all four verification-policy fields. It does not replace published image provenance with a source pin. These tests create public values and one bounded isolated import subprocess after implementation; they do not execute a provider or duplicate Secrets custody/HTTP/cryptographic validation. Root dependency checks and the normal numeric witness are later implementation-context additions, not a cause to mask the focused missing-product-interface red.

The graph public_environment remains exactly the historical value: Core rejects the accepted CPK_SECRETS_CONTROL_CONFIGURATION_FILE name as secret-shaped. Its fixed path belongs to Docker ENV and the public bootstrap ABI, while topology supplies the actual public artifact. No Core rule or service name is bypassed. The artifact-negative test additionally changes valid public JSON without updating immutable digest metadata and requires rejection, so a lawful payload cannot conceal a forged artifact descriptor.
