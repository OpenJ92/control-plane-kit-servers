Source: [source_control_fixture.py](../../../../../products/secrets_server/tests/source_control_fixture.py).
Maintain with the maintained smoke's fixed file and CLI boundaries.

generate reuses SourceControlAuthority and the actual product artifact factory.
It exclusively creates public control.json0444 and two private headers0600;
keys remain in memory. Stable run identity across phases preserves target/runtime
and declaration while generation creates fresh public keys and120-second grants.
No issuer service, Docker socket or provider authority is introduced.

check decodes the actual phase configuration and reconstructs Core requests
without new authority. Exact static canonical bytes, typed healthy liveness,
missing authority and wrong-purpose401 are checked. HTTP streaming uses five-
second operation timeouts and65536-byte accumulation without refresh/retry;
this is not a whole-request deadline.

logs requires successful separately recorded Docker producer status, bounded
regular no-follow capture and bounded private/header files. Existing markers,
exact client values and optional source tokens are checked without matching
output. Empty process logs are valid; missing/oversized input is not. Ordinary
CLI failures produce one fixed line. Partial generation leaves only enumerated
filenames for shell cleanup; existing files are never silently overwritten.
