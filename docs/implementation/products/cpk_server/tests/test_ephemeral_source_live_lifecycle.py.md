Source: [products/cpk_server/tests/test_ephemeral_source_live_lifecycle.py](../../../../../products/cpk_server/tests/test_ephemeral_source_live_lifecycle.py).
Maintain this document alongside its source file. When the source or relevant imported contracts change, verify and update this companion in the same change.

The graph-construction case selects ephemeral ingress using local descriptors.
Other cases inject epoch rows to reject repeated tunnel, DNS and token-version
identities, and use AST line order to require checkpoints after transitions
and before postcondition assertions. AST order does not establish transactional
checkpoint durability or runtime execution.

Mocked public-probe responses exercise fresh attempt IDs for unresolved
endpoints and HTTP 530 warmup, with the shared retry policy. The scripted MCP
response is accepted on selected status/key fields; it is not proof of an actual
target command or network path.

Retirement checks exclude named reservation helpers and retain named emergency
fallback hooks. Synthetic Cloudflare inventory summaries test equality and
public descriptor shape; shell text checks require inventory capture before
network creation and exclude system prune. These do not enumerate a real
account/daemon, prove exact cleanup ownership, or execute emergency compensation.

Related source: [hosted graph builder](../../../../../scripts/cpk_server_hosted_activity.py),
[custody launcher](../../../../../scripts/cpk_server_cloudflare_secret_custody_source_live_smoke.sh).

Controller owner: [source-live controller](../../../../../scripts/cpk_server_secret_provider_source_live.py).
