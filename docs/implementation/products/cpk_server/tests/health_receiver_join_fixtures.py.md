Source: [health_receiver_join_fixtures.py](../../../../../products/cpk_server/tests/health_receiver_join_fixtures.py).

Synthetic never-executed receiver-only products preserve actual parser slot/provider/surface facts. They deliberately omit the CPK database requirements and legacy HTTP verification; they are not full source products or supported deployment inputs. Full CPK contracts are tested separately. Actual Core graph validation/compiler and Operations target projection produce the selection context. BASE-side coverage uses an explicit typed side transformation of a compiled observation with the same immutable snapshot and preserved obligations; the real resolver checks that transformed selection. No ready flag or review activity is removed.

The product lookup is exact and read-only, returning real RegisteredProduct values. Signer constructors carry only synthetic unresolved references and public keys; this fixture assumes upstream key/approval admission and does not emulate it. Plan pin identifiers are explicit fixture metadata, not durable acceptance records. Private-key test material is generated locally inside tests and never reaches descriptors or logs. No fixture enters catalogue or process defaults.

`gateway_self_world` separately builds the complete actual #182 source contract
using real control/transit/relay artifacts, then canonicalizes and registers its
descriptor. The own binding has an arbitrary alias and an internal origin. Both
selected health trust artifacts contain public key B while registered defaults
contain A, exposing default substitution. This fixture does not invent an
admitted bootstrap plan, attempts, installed state or provider evidence.
