Source: [products/cpk_server/tests/test_root_bootstrap.py](../../../../../products/cpk_server/tests/test_root_bootstrap.py).
Maintain this document alongside its source file. When the source or relevant imported contracts change, verify and update this companion in the same change.

These focused tests exercise pure root planning and refusal paths with real
selected product documents, Core compilation and temporary private files.
The deterministic plan is compared with the shared installation graph,
including CPK-only runtime access and the projected local socket/group marker,
resource names, unresolved material references and external endpoint.

Tampering with network projection, sibling socket permission or driver ID is
refused before a receipt is created. Permissive material fails before
acquisition and omits the fixture secret from the exception text. The latter
checks this refusal message, not every provider/log/trace surface.

An already pending receipt is inspected without a daemon, then prevents apply
despite missing material; its bytes remain unchanged. This witnesses a local
refusal to redispatch, not an interrupted real Docker run, full acquisition,
public setup, cleanup, or fresh readiness test. Live root witnesses own those
distinct observations.

Related source and evidence: [products/cpk_server/src/control_plane_kit_servers_cpk_server/bootstrap.py](../../../../../products/cpk_server/src/control_plane_kit_servers_cpk_server/bootstrap.py), [products/cpk_server/src/control_plane_kit_servers_cpk_server/bootstrap_runtime.py](../../../../../products/cpk_server/src/control_plane_kit_servers_cpk_server/bootstrap_runtime.py), [products/cpk_server/src/control_plane_kit_servers_cpk_server/installation.py](../../../../../products/cpk_server/src/control_plane_kit_servers_cpk_server/installation.py), [products/cpk_server/tests/live_root_bootstrap.py](../../../../../products/cpk_server/tests/live_root_bootstrap.py).
