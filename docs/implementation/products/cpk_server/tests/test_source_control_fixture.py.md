Source: [test_source_control_fixture.py](../../../../../products/cpk_server/tests/test_source_control_fixture.py).
Maintain alongside the owned source smoke fixture.

The generated directory must contain exactly public configuration and two private
headers, with0444/0600 modes and no private key. The health JWT is bounded and its
240-second lifetime is inspected, then the actual SDK verifier admits its signed
request with the generated authority and exact receiver identity. Exclusive
creation must reject reuse. Source/published profile wiring, actual generator and
verification invocations, mount/HTTP bounds and every exact cleanup filename are
asserted because they are the shell's new fixture ABI. These tests execute only
inside the existing normal Docker-backed ./test.sh.
