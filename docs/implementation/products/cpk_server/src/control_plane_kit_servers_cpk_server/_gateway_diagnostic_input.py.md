Source: [_gateway_diagnostic_input.py](../../../../../../products/cpk_server/src/control_plane_kit_servers_cpk_server/_gateway_diagnostic_input.py).
Maintain this companion with its source and selected contracts.

Offline closed64KiB canonical packet parsing joins four fixed public inputs: actual
canonical product descriptor plus three selected gateway artifacts. Existing
codecs, declared unique slots, source runtime contract and matching functions
bind exact self target/runtime/readiness/V2/alias and transit ingress. No fabricated
Operations realized-projection value is constructed. Public keys and original
grants agree with independently selected configured facts; timing during execution
is checked by the caller and real signer/client. Packet digest is integrity, not
authority. Plan output excludes grants/private origins/reference routing and does
not read private bootstrap, database or network. Actual readiness remains unknown.
