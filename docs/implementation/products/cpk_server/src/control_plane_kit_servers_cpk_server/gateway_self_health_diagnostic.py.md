Source: [gateway_self_health_diagnostic.py](../../../../../../products/cpk_server/src/control_plane_kit_servers_cpk_server/gateway_self_health_diagnostic.py).
Maintain this companion with its source and selected contracts.

#221 composes trusted existing credential verification and SECRET_PROVIDER_USE
policy with existing Operations authorization/projection. Candidate claims cannot
provide actor/scopes, resolution grants, database routes or private file paths.
The independently protected approval binds canonical packet digest, principal,
workspace, original attempt, database identity and maximum expiry. Trusted setup
provenance is a reviewed invocation assumption, not inferred from a filename.

Ordered workspace/attempt/family correlation locks precede both existing-record
checks. Either prior record refuses. Current registered signer and returned
custody routing must match approved selections. Both real authorizations share
explicitly diagnostic correlation labels, not persisted deployment row claims.
The actual authorizer owns IDs/fingerprints/admission. A commit request is followed
by successful UOW exit before effects; exit ambiguity produces no key/health I/O.
A cancellation checkpoint precedes the synchronous149 signer. Approval is reread
and original windows checked before signing and again before147 dispatch.

No retry or partial credential output. Rows remain after postcommit failure;
authorized-not-dispatched excludes health sending but may follow Secrets I/O.
No SQL/schema/registration/key provisioning, graph advancement or deployment
history is created. Recording-UOW tests establish adapter ordering only; real
Postgres locking/atomicity remain Operations laws. Source/live evidence is recorded
in PR222; this module does not itself establish live authority or resource custody.
