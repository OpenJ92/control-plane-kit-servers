Source: [__init__.py](../../../../../../products/http_multiplexer/src/control_plane_kit_servers_http_multiplexer/__init__.py).
Maintain with the pure package entrance.

Settings/error exports come from product-local configuration. Existing main remains
callable through lazy process delegation. Value imports do not load the HTTP
process, read startup files/environment or bind sockets. Server-level settings
imports retain the same actual nominal type.
