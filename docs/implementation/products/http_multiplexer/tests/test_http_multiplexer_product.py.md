Source: [test_http_multiplexer_product.py](../../../../../products/http_multiplexer/tests/test_http_multiplexer_product.py).
Maintain with the ten inherited product laws.

All ten tests/claims remain. Only source assertions for the primary/observer
environment names follow their actual configuration owner; forwarding bounds and
fail-open checks remain on server.py. Historical descriptor identity/digest,
required primary/optional observers, explicit settings18082, helper forwarding and
redacted observer failures are preserved. Installed-host composition is strengthened
in the new wrapper suite rather than weakening these existing assertions.
