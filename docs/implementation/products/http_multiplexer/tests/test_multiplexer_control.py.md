Source: [test_multiplexer_control.py](../../../../../products/http_multiplexer/tests/test_multiplexer_control.py).
Maintain with the focused product receiving and installed-host laws.

Focused law commit f5945e9 precedes application implementation; no compulsory
executable-red harness was run. Eight new tests cover signed canonical liveness,
authentic undeclared readiness and representative reserved denials before either
forwarding lane, independent of ordinary /health/ready and application auth.
Installed-host tests strengthen inherited helper-only checks: primary failure
skips observers; after primary success an observer failure still permits the next
observer and preserves primary response. Call order, copied method/path/query/
body/headers, zero observers and per-lane bounds/timeout/no-redirect rules survive.
No latency-independence or durable delivery assertion is made.

Other laws cover real config/artifact/source codecs, complete historical fields
and verification policy, fixed-file opened identity and negative cases, bounded
closed trust/locality, fixed errors, lifecycle closure/production port policy and
two-instance settings/key isolation. Standalone tuple flexibility survives while
mutable/invalid observer shapes reject. Existing tests evict product modules, so
new cases refresh nominal module references. Pure-import subprocesses run inside
the owning Docker test container. Only the established ./test.sh provides
executable evidence; no SDK admission matrix or alternate runner is introduced.
