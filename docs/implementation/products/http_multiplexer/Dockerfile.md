Source: [Dockerfile](../../../../products/http_multiplexer/Dockerfile).

Early #181 compatibility for #208 selects Coreb79a02d1, Operationsbc559a0, Interpreters6b1c75a9, SDK2c5b5882 and Secrets8273b7de through independent canonical Core/Operations fields. The ordinary gate checks generated bytes and installed provenance; this is source dependency adoption, not image publication or production health execution. Historical product descriptors/image coordinates are unchanged.
Maintain with the canonically generated SDK runtime dependency.

Python3.12 installs SDK[verification] at the canonical coordinate through the
existing generator. UID10004 and PORT/EXPOSE8000 remain. No default config/private
keys are baked in. This source recipe does not qualify/publish an image;191 owns
candidate qualification with explicit receiving configuration and owned cleanup.
The legacy standalone smoke is not invoked or repaired by this wrapper slice.
