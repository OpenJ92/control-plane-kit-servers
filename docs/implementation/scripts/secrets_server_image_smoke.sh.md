Source: [secrets_server_image_smoke.sh](../../../scripts/secrets_server_image_smoke.sh).
Maintain with the owning product recipe, private bootstrap and image profiles.

During203's intermediate source adoption, default/explicit BUILD_IMAGE=1 exits2 with an explicit204 message before any Docker call or trap installation. It cannot fall back to an old image or start resource cleanup.204 must replace this temporary hold with a functioning reviewed wrapped-source path before189 closes; this is not permanent retirement or qualification191's work.

Explicit BUILD_IMAGE=0 keeps the original immutable @sha256 requirement and historical provider bootstrap/private file/data-volume workflow, initial write/generate/resolve/revoke and restart checks, redaction and label-owned cleanup. Its code after the early hold is unchanged. Cleanup suppression is followed by an explicit residue check; it is not general cleanup authority. This baseline does not exercise the new receiving ABI. The ordinary test.sh continues selecting this explicit historical mode; the numeric source stage separately owns actual new source composition.
